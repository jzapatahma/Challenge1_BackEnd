## // ** realizamos la creacion de las variables de entorno en windows, para evitar mostrar la configuracion y valores de las credenciales en BD
#spring.application.name=biblioteca
#spring.datasource.url = jdbc:postgresql://${DB_HOST}/${DB_NAME}
#spring.datasource.username = ${DB_USER}
#spring.datasource.password = ${DB_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver
#hibernate.dialect=org.hibernate.dialect.HSQLDialect

#spring.jpa.hibernate.ddl-auto=update

#spring.jpa.show-sql = true
3spring.jpa.format-sql = true

# Para ver los log de transacciones en base de datos debemos adicionar lo siguiente:


# // ** este primer bloque lo realizamos al inicio de manera abierta pero no quedan seguros.
spring.application.name=biblioteca
spring.datasource.url = jdbc:postgresql://localhost:5432/bibliotecas
spring.datasource.username = postgres
spring.datasource.password = 123
spring.datasource.driver-class-name=org.postgresql.Driver
hibernate.dialect=org.hibernate.dialect.HSQLDialect

spring.jpa.hibernate.ddl-auto=update

#spring.jpa.show-sql = true
#spring.jpa.format-sql = true